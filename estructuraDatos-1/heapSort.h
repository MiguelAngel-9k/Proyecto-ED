#pragma once
#include "Patient_class.h"

namespace MAX_SORT{
	//void heapify(Patient* arr, int n, int i);
	//void heapSort(Patient* arr, int n);
	//bool print(Patient* arr);
	//void swap(Patient*,int, int);
}
